const userName = 'Max';
let age = 30;

function add(a: number, b: number) {
  var result;
  result = a + b;
  return result;
}

if (age >= 20) {
  var isAdult = true;
}
// console.log(isAdult); // アクセスできてしまう

